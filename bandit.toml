[tool.bandit]
# Note: severity gating is handled in the CI via the -lll flag to only fail on HIGH severity.
# This file is retained for future plugin configuration, test exclusions (skips), or profiles.
