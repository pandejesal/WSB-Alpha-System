# Feature-Gap Matrix — WSB-Alpha-System vs Comparable Repos

Date: 2026-08-09
Companion to: `docs/research/COMPARABLE-REPOS.md` (all repo URLs and feature claims there).
Legend: **Present** = feature exists in our src. **Partial** = exists in weakened form. **Absent** = no code (verified by grep; see Evidence column).
Rules honored: no edits to `src/backtest/validators/statistical.py`, `src/backtest/permutation_tester.py`, `src/risk/fred_macro_provider.py` (other-owner). Every borrow item is a PROPOSAL, not code.

| # | Feature | Ours (present? — src path / evidence) | Repo exemplars (what they have) | Value to us |
|---|---------|---------------------------------------|-----------------------------------|-------------|
| 1 | **Multi-provider data fallback** | **Partial.** `src/data/providers/yfinance_provider.py`, `openbb_provider.py` (fallback exists via `src/data/providers/base.py` + `src/data/cache_engine.py`), but yfinance is primary in 24 files / 60 call sites (`src/alpha/macro_regime.py:25`, `src/execution/live_alpaca_executor.py:209`, `src/data/market_data.py:36`). No ordered chain (Alpaca -> Tiingo -> Binance -> yfinance), no provider health/heartbeat. | FinRL-X auto-selects best source (FMP > WRDS > Yahoo) in `src/data/data_fetcher.py`; LLMStrat: CRSP-first with EODHD fallback; c5-sentiment-aggregator: FinBERT with VADER fallback, graceful degradation. | **High** — our own synthesis names it "the biggest item"; it is upstream of everything else. |
| 2 | **Order reconciliation / idempotent order lifecycle** | **Absent** — `grep reconcil|client_order|idempoten` over `src/` -> 0 hits. Executors (`src/execution/alpaca_broker.py`, `ccxt_broker.py`, `universal_broker.py`, `execution_wrapper.py`) submit orders but have no client-order-id dedup, no partial/unknown-fill reconciliation, no 5xx -> reconcile retry. | alpatrade: dedicated Reconciler agent (DB vs Alpaca positions/orders) + startup order sync; algo-trader: journal-replay reconciliation on boot; LLMStrat: post-trade reconciliation checks; zinan92/executor: place -> confirm -> fill -> journal lifecycle + dry_run. | **High** — paper fills ~100% but live <40% (synthesis #7); this is the paper-to-live credibility gap. |
| 3 | **Trial ledger + Deflated Sharpe (selection-snooping control)** | **Absent** — `grep -i 'deflat\|trial.ledger\|dsr' src` -> zero hits. `docs/data/strategy_rankings.json` has `likely_overfit` flags but no per-trial accounting; `src/backtest/permutation_tester.py` and `validators/statistical.py` are pre-owned; `src/backtest/whites_reality_check.py` exists, but nothing counts total experiments (~90 variants per README) against the promoted best. | deflated-sharpe (DSR + min-backtest-length + RegimeDecayDetector); oos-lab (PSR/DSR/PBO/CPCV-with-embargo/Harvey-Liu haircut); backtest-audit (DSR + MC + regime audit -> PASS/WARN/FAIL); pypbo (PBO/PSR/DSR/MinTRL/MinBTL); sharpebench (SPA + Romano-Wolf). | **High** — the literature-standard fix for "90 strategies tested, 0 robust". |
| 4 | **Regime governor (vol-target / VIX / gating)** | **Partial.** `src/alpha/macro_regime.py` = SMA(200) BULL/BEAR only; `src/alpha/smc_alpha_generator.py` gates longs/shorts by regime tag; no VIX percentile, no vol-target sizing, no dual-speed (slow trend + fast shock) mode, no live decay monitor. | FinRL-X: 26-week trend + VIX regime + 3-day fast risk-off shock, cooldowns, daily stop monitoring; algo-trader: `macro_regime_filter` VIX + 200-DMA used as exposure scalar; deflated-sharpe: RegimeDecayDetector (3-signal live decay); agentic-trader: HMM regime detector. | **High** — backed by our own `web-research/regime-detection.md`; vol-targeting has the strongest evidence. |
| 5 | **Indicator catalog / warmups** | **Partial.** `src/alpha/indicators.py` has the main set, unit-tested (`tests/test_indicators.py`), but no warmup table, no range-vol estimators (Parkinson/GK/RS), no MFI+MACD combos vs our own 36-indicator gap-list (synthesis #12). | agentic-trader: Qlib factor library with lagged (leakage-safe) features; alpha-engine: 15 indicators computed from scratch with provenance; no competitor has a rigorous warmup catalog — this is ours to define. | **Medium** — quality-of-signals; upstream of alpha but not a headline gap. |
| 6 | **Execution parity (paper vs live behavior)** | **Partial.** Paper fills ~100%, live ~<=40%, partial/unknown IOC semantics undocumented (synthesis #7; needs line-audit). `src/execution/base_broker.py` + adapters exist; no dry-run local simulation; broker-documented divergences (Alpaca paper does NOT simulate dividends; fill timing differs) not modeled. | zinan92/executor: local dry_run fills; alpatrade: startup order sync; Alpaca paper-trading docs enumerate the paper/live differences; rustrade: per-broker parity as a design goal. | **Medium** — full fix belongs to item #2; parity *measurement* is cheap on its own. |
| 7 | **Dashboard** | **Present.** Static Pages dashboard `docs/index.html` + `src/monitoring/dashboard.py` (+ Telegram bot); no live order stream, no approval/kill UI. | algo-trader: real-time Next.js dashboard (11 endpoints, WebSocket signal/fill/halt fan-out), FLATTEN kill switch; agentic-trader: live 15-portfolio leaderboard; CubeXAgents: WebSocket HITL approve/reject UI; Agentictrading: control-tower. | **Medium** — ours is fine for the free tier; the real win is the kill-switch/HITL item below. |
| 8 | **Skills / LLM-agent integration** | **Strong/Partial.** `src/research/skill_executor.py`, `agent_skills_registry.py`, `self_learning_agent.py`, `debate_engine.py`, `memory_engine.py`, `src/utils/gemini_provider.py` — free Gemini self-learning loop. | TradingAgents: full LangGraph multi-agent debate; QuantChain: LangGraph + RAG + vector memory; ai-trading-framework: plugin layer + Telegram operator; LLMStrat: *constrained* factor ideation (exact model, sanitizer, token budgets). | **High** — we are ahead of most free-tier repos on self-learning, but behind on *constraint* (LLMStrat) and on "LLM as governance, not oracle" (algo-trader). |
| 9 | **Walk-forward + embargoed CPCV** | **Partial+.** `src/backtest/walk_forward_engine.py`, `src/backtest/optimization/walk_forward.py`; CPCV exists in `validators/statistical.py:78` but purge-only, **no embargo** (locked file — must wrap at call sites). | oos-lab: `CombinatorialPurgedKFold` with embargo; eslazarev/purged-cross-validation: Purge + Embargo + CPCV + CSCV (sklearn-compatible); algo-trader: "purged + embargoed walk-forward (Lopez de Prado)". | **High** — nothing to build, only to plug in at the promotion layer. |
| 10 | **Portfolio risk gates** | **Partial.** `src/risk/position_sizer.py`, `position_sizing.py`, `circuit_breakers.py`, `portfolio_manager.py`, `portfolio_optimization.py`; caps in `config/risk_config.py`. No account-level DD halt threshold, no $100-basis heat cap, no two-gate (risk + compliance) convention, no Monte-Carlo DD envelope, no min-trade-count preconditions. | algo-trader: hard caps (1% / 6% / 10% / -2% daily / -15% DD) with tests forbidding loosening; two-gate execution; correlation caps (<=0.7 -> kill weaker); Agentictrading: fenced paper/live via deterministic gate. | **High** — matches synthesis #8 (drawdown guardrail) and is essential for a $100 account. |
| 11 | **No-record-no-order decision journal** | **Absent** — no decision journal; plain logging only. | algo-trader: append-only JSONL, fsync + secret redaction, "no record, no order"; alpatrade: Postgres ledger; Agentictrading: Goldfish durable experiment memory + lineage. | **Medium** — gives audit trail and reconciliation input; cheap. |
| 12 | **Human safety: kill switch / approval** | **Partial** — Telegram bot notifications only; no interactive kill or approval injection. | algo-trader: FLATTEN kill switch; agentic-trader: FORCE_FLATTEN halt; ai-trading-framework: approval-first + Telegram approve/reject; CubeXAgents: HITL gate; IntelliStock: paper-first containerized instances. | **High** — with real capital (even $100) a human pedal beats cutting the API key. |
| 13 | **Leakage firewall test-suite** | **Partial** — no single gate for feature leakage; some engines have lookahead asserts. | statistical-grounded-trader: `tests/test_leakage.py` ("no feature row can see future"); agentic-trader: `tests/test_qlib_leakage.py`; LLMStrat: data-QA gates. | **Medium-High** — cheap, high-credibility, often a drop-in. |
| 14 | **Promotion gate abstraction** (backtest -> paper and paper -> live as measurable multi-criteria gates) | **Partial** — `src/backtest/incubation_manager.py` + `src/evolution/strategy_selector.py` exist but selection is not multi-criteria-gated. | algo-trader: promotion.py 7 criteria + check_live_ready.py 9 criteria; Agentictrading: per-lineage deterministic gate; FinRL-X: published paper/live results with thresholds. | **Medium** — rests on items #3 and #10 foundations. |

Evidence note: every "Absent" above was verified with grep over `src/` on 2026-08-09; every "Partial" cites the exact src path.

## Priority conclusion

Highest-value, lowest-effort order: **#3 (DSR + trial ledger)** -> **#2 (order reconciliation)** -> **#4+#10 (regime governor + DD guardrail)**. Items #11/#12/#13 are cheap enablements of the same literate promotion path. This aligns 1:1 with the P0-P1 backlog in `web-research/synthesis.md`.
