family: tail_risk_sentinel
run_id: 20260911-h9-tailsentinel
variant: tail_risk_sentinel_h9
universe: "SPY + duration/metals defensive sleeve (TLT, GLD, SLV, HYG, UUP — all present in market_data_2019_2026/ohlcv/, T=1910 bars 2019-01-02..2026-08-07); identical-window SPY twin for excess; local CSVs ONLY, no network; NO VIX input, NO ATR stop, NO realized-vol cap"
hypothesis: "An ALWAYS-INVESTED stress rotation on SPY (tmin=1.00 by construction: 100% SPY in calm; 100% equal-weight duration/metals-4 under stress, NEVER cash) with a LOOSER single-trigger OR-threshold (SPY<SMA200 alone OR trailing-60d<-8% alone, vs H7's -10%) fires MORE stress episodes and a DD CAP on the INTO-leg (when the sleeve's own equal-weight trailing-60d DD exceeds 10%, scale the INTO-leg to 0.5x with the remainder in SPY) cuts SPY buy-hold maxDD to <=25% while holding identical-window excess >= +0.03pp: looser trigger harvests more crisis alpha, the cap stops the sleeve itself from dragging the portfolio through correlated duration/metals drawdowns (H7 RECORDED cycle22: sharpe 1.066/dd 0.2851/excess -63.30pp/dsr 0.7344/trips 18/perm 0.14 FAIL; H5 defensive-8 dd 0.5214 FAIL). Costs are switch + monthly-reselect turnover only (tiered W5). Single frozen combo; untried vs TABOO: H7 had tighter -10% trigger + NO cap; H5 had equity-heavy defensive-8 sleeve; H1 VIX-grid triple, H3 SMA-ladder combo retired."
acceptance: "Edge claim pre-registered via preregister freeze BEFORE backtest and verified; YAML validates via strategy_registry.validate_spec; Walk-Forward OOS>=0.35, Sharpe>=0.55, maxDD<=25%, excess>=0.03pp, DSR>=0.75 (per-family N=55), trips>=6, perm_p<=0.05, boot_p<=0.05 (GATESPEC38 Track 4 tail_risk_sentinel); fallback Track 2 drawdown_warrior (DD<=35%, excess>=0.10); L1 Sharpe>=0.50 DD<15% excess>0; WF 3-fold overlay mean fold OOS>=0.35; CPCV-proxy overlay (4 blocks/6 paths, no embargo, disclosed); verdict recorded via preregister record (HONEST_ABANDON allowed)."
kill_criteria: "Falsified in-sample (excess<0.03pp or DD>25%); fails WF OOS or perm/boot (p>0.05); 0 tracks cleared -> HONEST_ABANDON, combo retired to TABOO ledger, no silent overwrite."
dedup: "Registry tail rows (tail_risk_sentinel_v4 inactive, tail_risk_sentinel_h5 inactive) contain no -8%-trigger and no DD-capped sleeve; H7 (-10% OR-trigger, no cap, cycle22 HONEST_ABANDON) and H5 (defensive-8, cycle20 HONEST_ABANDON) differ on both changed dimensions; H1 VIX-grid triple and H3 SMA-ladder retired. NOTE: _quant_improve/LOOP-5H-strategy-hunt.md TABOO ledger absent from this checkout (no _quant_improve/ dir); dedup performed against registry + H1/H3/H5/H7 briefs + cycle20/cycle22 prereg/eval docs instead."
edge_gate_params:
  track: tail_risk_sentinel
  primary_track: tail_risk_sentinel
  fallback_track: drawdown_warrior
  sharpe_min: 0.55
  max_dd_max: 0.25
  oos_min: 0.35
  excess_min: 0.03
  dsr_min: 0.75
  trips_min: 6
  perm_p_max: 0.05
  boot_p_max: 0.05
  fallback_thresholds:
    sharpe_min: 0.55
    max_dd_max: 0.35
    oos_min: 0.35
    excess_min: 0.10
    dsr_min: 0.75
    trips_min: 6
  h9_params:
    sma_slow: 200
    drawdown_trigger_60d: -0.08
    beta_lookback: 252
    defensive_k: 4
    defensive_sleeve: [TLT, GLD, SLV, HYG, UUP]
    selection: monthly_month_end
    calm_leg: SPY_100pct
    stress_leg: duration_metals4_equal_weight
    dd_cap: "sleeve trailing-60d DD > 10% -> INTO-leg 0.5x, remainder SPY (still always-invested)"
    exec_delay: 1
    fam_trials_N: 55
  banned_triple: "VIX-level gate + ATR stop + vol-cap together (H1 v1..v4 grid retired family-TABOO); H9 uses NONE of the three. H3 SMA(50,100,200) graduated levels 1.00/0.65/0.30/0.00 retired; H9 has no cash leg. H5 broad-panel defensive-8 (k=8, 441-name lowest-beta-252) retired TABOO after 52% DD; H9 uses fixed 5-name duration/metals sleeve, k=4. H7 (-10% OR-trigger, no cap) HONEST_ABANDON cycle22; H9 loosens to -8% AND adds DD cap."
  cost_model: "tiered W5 (2.5bp base + vol term) on both legs, T+1 via weight shift(1)"
  benchmarks: ["SPY identical window", "WF 3-fold", "CPCV-proxy 4 blocks/6 paths", "circular-shift perm K=200", "stationary bootstrap block21"]
